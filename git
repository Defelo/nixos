[user]
	email = elodef42@gmail.com
	name = Defelo
	signingkey = 61303BBAD7D1BF74EFA44E3BE7FE2087E4380E64
[commit]
	gpgsign = yes
[diff]
	submodule = log
[tag]
	gpgSign = yes
[alias]
	l = log --graph

{{#if git.delta}}
# https://github.com/dandavison/delta
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[add.interactive]
    useBuiltin = false # required for git 2.37.0
[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
    side-by-side = false
    syntax-theme = Nord
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
{{/if}}
